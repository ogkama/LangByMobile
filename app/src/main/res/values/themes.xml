<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LearnByMobile" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorOnBackground">@color/white</item>
        <item name="colorPrimary">@color/dark_background</item>
        <item name="colorBackgroundFloating">@color/q5</item>
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.LearnByMobile" parent="Base.Theme.LearnByMobile" />
    <style name="Theme.LearnByMobile.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/accent</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/logo</item>
        <item name="postSplashScreenTheme">@style/Theme.LearnByMobile</item>
    </style>>
</resources>